/// @description Insert description here
// You can write your code in this editor

/*
if(place_meeting(x,y,obj_player)) // check if the player is near the npc to have conversation
{	
	if(keyboard_check_pressed(vk_space)){	//press spacebar to start the conversation
	if(currentTextBox = noone){
	currentTextBox = instance_create_layer(x,y,"Text_Box",obj_TextBox);
	currentTextBox.text = NPC_text;
		}
	}
} else{
	if(currentTextBox !=noone){			//delete the textbox if the player leave the npc 
	instance_destroy(currentTextBox);
	currentTextBox = noone;
	}
}/**/



event_inherited();